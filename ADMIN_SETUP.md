# ระบบหลังบ้าน MCCMU — คู่มือติดตั้งและส่งต่อ

ระบบนี้ให้ฝ่ายไอทีแก้ไขข้อมูลบนเว็บได้จากหน้า dashboard โดยไม่ต้องแตะฐานข้อมูลตรง ๆ

| ส่วนประกอบ | ใช้อะไร |
|---|---|
| หน้าเว็บ | HTML/CSS/JS ธรรมดา deploy บน Vercel |
| ฐานข้อมูล | Supabase (Postgres) |
| ล็อกอิน | Supabase Auth ผ่านบัญชี Google |
| ไฟล์/รูป | Google Drive (จะย้ายมา Supabase Storage ในเฟสถัดไป) |

---

## ตั้งค่าครั้งแรก

### 1. โปรเจกต์ Supabase

โปรเจกต์ปัจจุบัน: **MCCMU's Project** · region `ap-southeast-1` (สิงคโปร์)

โครงสร้างฐานข้อมูลอยู่ในโฟลเดอร์ `supabase/migrations/` รันตามลำดับเลข
ถ้าต้องสร้างโปรเจกต์ใหม่ก็รันสองไฟล์นี้ซ้ำได้เลย

> **แผนฟรีให้ 2 โปรเจกต์เท่านั้น** ถ้ามีโปรเจกต์เก่าที่เลิกใช้แล้วให้ลบทิ้ง
> ไม่งั้นจะสร้างโปรเจกต์ใหม่ไม่ได้เมื่อต้องการ

### 2. เปิดล็อกอินด้วย Google

ต้องตั้งค่า 2 ฝั่ง — ฝั่ง Google ก่อน แล้วเอาค่ามาใส่ฝั่ง Supabase

**ฝั่ง Google Cloud Console** ([console.cloud.google.com](https://console.cloud.google.com))

1. สร้างโปรเจกต์ใหม่ (หรือใช้ของเดิม) — ใช้บัญชีกลางของชมรม ไม่ใช่บัญชีส่วนตัว
2. **APIs & Services → OAuth consent screen**
   - เลือก **External**
   - App name: `MCCMU Admin`
   - User support email + Developer contact: อีเมลชมรม
   - บันทึก (ไม่ต้องส่ง verification เพราะใช้กันในกลุ่มเล็ก)
3. **APIs & Services → Credentials → Create Credentials → OAuth client ID**
   - Application type: **Web application**
   - **Authorized redirect URIs** ใส่:
     ```
     https://bzbqzohygnraqquabffg.supabase.co/auth/v1/callback
     ```
   - กด Create แล้ว **คัดลอก Client ID กับ Client Secret เก็บไว้**

**ฝั่ง Supabase**

4. Dashboard → **Authentication → Sign In / Providers → Google**
5. เปิด **Enable Sign in with Google**
6. วาง **Client ID** และ **Client Secret** จากขั้นที่ 3
7. กด Save

**ตั้ง URL ปลายทางหลังล็อกอิน**

8. **Authentication → URL Configuration**
   - Site URL: `https://<โดเมนเว็บของชมรม>`
   - Additional Redirect URLs: ใส่ `http://localhost:8899/admin` ด้วย ถ้าจะทดสอบในเครื่อง

> **Client Secret เป็นความลับ** อย่าใส่ในโค้ดหรือ commit ขึ้น GitHub
> ใส่ในหน้า Supabase เท่านั้น

### 3. เพิ่มคนที่เข้าหลังบ้านได้

สิทธิ์ดูจากตาราง `admins` ในฐานข้อมูล — **ไม่ได้อยู่ในโค้ด**
เปลี่ยนรุ่นแค่เพิ่ม/ลบแถวในตารางนี้ ไม่ต้องแก้โค้ดหรือ deploy ใหม่

คนแรกต้องใส่ผ่าน SQL Editor ใน Supabase (เพราะยังไม่มีใครมีสิทธิ์เข้าหน้า admin):

```sql
insert into admins (email, name, note, role)
values ('อีเมล@gmail.com', 'ชื่อ นามสกุล', 'หัวหน้าฝ่ายไอที 2569', 'owner');
```

หลังจากนั้น **เจ้าของระบบ** เพิ่มคนอื่นผ่านหน้า dashboard ได้เลย

**อีเมลต้องตรงกับบัญชี Google ที่ใช้ล็อกอิน** และระบบเทียบแบบตัวพิมพ์เล็กทั้งหมด

### ระดับสิทธิ์ (migration 0006)

| ระดับ | แก้เนื้อหา | เพิ่ม/ถอด/เปลี่ยนระดับผู้ดูแล |
|---|---|---|
| `owner` เจ้าของระบบ | ได้ | ได้ |
| `editor` ผู้แก้ไข (ค่าเริ่มต้น) | ได้ | **ไม่ได้** (เห็นรายชื่ออย่างเดียว) |

บังคับด้วย Row Level Security ไม่ใช่แค่ซ่อนปุ่ม — `editor` ที่ยิง REST เข้ามาตรง ๆ
เพื่อลบผู้ดูแลคนอื่นก็ยังโดนปฏิเสธ และทุกการเปลี่ยนแปลงในตาราง `admins`
ถูกบันทึกลง `audit_log` ให้ย้อนดูได้ว่าใครถอดใครออกเมื่อไหร่

ตอนรัน migration 0006 ครั้งแรก **คนที่ถูกเพิ่มเข้าระบบเป็นคนแรกจะกลายเป็น `owner`
ที่เหลือเป็น `editor` ทั้งหมด** ถ้าไม่ตรงกับความจริงให้แก้ใน SQL Editor:

```sql
update admins set role = 'owner' where email = 'อีเมล@gmail.com';
```

ฐานข้อมูลกันไม่ให้เหลือเจ้าของ 0 คน (ตั้งคนใหม่ก่อนแล้วค่อยถอดคนเดิม)
ถ้าเจอ error `ต้องเหลือเจ้าของระบบอย่างน้อย 1 คนเสมอ` แปลว่ากันไว้ตรงนี้

---

## สลับแหล่งข้อมูล (Sheets ↔ Supabase)

`api/data.js` อ่านได้ทั้งสองแหล่ง เลือกด้วยตัวแปรเดียวบน Vercel

| ตัวแปร | ค่า | ผล |
|---|---|---|
| `DATA_SOURCE` | `sheets` (ค่าเริ่มต้น) | หน้าเว็บอ่านจาก Google Sheets เหมือนเดิม |
| `DATA_SOURCE` | `supabase` | หน้าเว็บอ่านจาก Supabase |

ต้องตั้งเพิ่มเมื่อใช้ Supabase: `SUPABASE_URL` และ `SUPABASE_ANON_KEY`
(ทั้งคู่เป็นค่าสาธารณะ ปลอดภัยที่จะใส่)

**ทดสอบก่อนสลับจริง** — ต่อ `?src=supabase` ท้าย URL จะเห็นข้อมูลจาก Supabase
โดยที่คนทั่วไปยังเห็นของเดิม:

```
https://<โดเมน>/data?sheet=all&src=supabase
```

**ก่อนสลับต้องรันสคริปต์นี้ให้ผ่านก่อน** ตรวจว่ารูปร่าง JSON จากสองแหล่งเหมือนกัน
ถ้าไม่เหมือนหน้าเว็บจะพัง:

```
SUPABASE_URL=... SUPABASE_ANON_KEY=... ./scripts/compare-sources.sh
```

**ถ้าสลับแล้วมีปัญหา** เปลี่ยน `DATA_SOURCE` กลับเป็น `sheets` แล้ว redeploy
ใช้เวลาไม่ถึงนาที ไม่ต้องแก้โค้ด Google Sheets ยังอยู่ครบเหมือนเดิม

---

## คำเตือนจาก Security Advisor ที่ "ตั้งใจให้เป็นแบบนั้น"

Supabase มีหน้า Advisors คอยเตือนเรื่องความปลอดภัย มี 2 ข้อที่จะขึ้นตลอด
และ **ไม่ต้องแก้** — อ่านให้จบก่อนจะไปเปลี่ยนอะไร

### `track_view` เป็น SECURITY DEFINER ที่ anon เรียกได้

เตือนว่า "ฟังก์ชันนี้คนที่ยังไม่ล็อกอินก็เรียกได้"

**เป็นเจตนา** เพราะเป็นทางเดียวที่หน้าเว็บสาธารณะจะนับผู้เข้าชมได้
ฟังก์ชันถูกจำกัดไว้แน่นแล้ว:

- ปัก `search_path` กันการสวมรอย
- ตรวจ path ด้วย regex + จำกัด 120 ตัวอักษร
- ไม่มี dynamic SQL
- เพิ่มได้ทีละ 1 ในตารางเดียว และคืนค่า `void` ไม่คืนข้อมูลอะไรเลย

**อย่าเปลี่ยนเป็น SECURITY INVOKER** เพราะจะต้องเปิดให้ anon เขียนตาราง
`page_stats` ตรง ๆ แทน ซึ่งแปลว่าใครก็ตั้งยอดเป็นเท่าไหร่ก็ได้ — แย่กว่าเดิม

อีกทางคือย้ายการนับไปฝั่ง Vercel แล้วใช้ `service_role` key
**ไม่แนะนำ** เพราะต้องเอา key ที่ข้ามความปลอดภัยได้ทั้งหมดเข้ามาในระบบ
ถ้าหลุดขึ้น GitHub เมื่อไหร่ = ฐานข้อมูลเปิดหมด ไม่คุ้มกับการไล่คำเตือน 2 บรรทัด

**ข้อจำกัดที่ยอมรับ:** ใครก็ยิงซ้ำให้ตัวเลขพองได้ เหมือน analytics ทุกตัว
ที่นับจากฝั่งเบราว์เซอร์ — ใช้ดูแนวโน้มได้ อย่าใช้อ้างอิงเป็นทางการ

### สิ่งที่ควรปิดจริง ๆ

**Authentication → Sign In / Providers → Email → ปิด Enable Sign Ups**

ถ้าเปิดไว้ ใครก็สร้างบัญชีในระบบได้ ถึงจะทำอะไรไม่ได้เพราะ RLS กันอยู่
(ทุก policy เช็คว่าอีเมลอยู่ในตาราง `admins` หรือไม่) แต่ก็ไม่มีเหตุผลให้เปิดทิ้งไว้
เพราะเราใช้ Google login อย่างเดียว

ปิดแล้วคำเตือน "Leaked Password Protection Disabled" จะหมดความหมายไปด้วย

---

## เรื่องที่ยังค้างอยู่ (สำคัญ)

### รูปในอัลบั้มต้องดึงเข้าระบบเอง (ไม่ใช่แค่ใส่โฟลเดอร์)

สมัย Apps Script แค่ใส่ `folder_id` แล้ว backend ไล่อ่านรูปในโฟลเดอร์ Drive ให้เอง
Supabase ทำแบบนั้นไม่ได้ **รูปที่ขึ้นหน้าเว็บอ่านจากตาราง `album_photos` เท่านั้น**

แปลว่าอัลบั้มที่สร้างใหม่แล้วใส่แค่โฟลเดอร์ Drive จะขึ้นเว็บเป็น "ปกอย่างเดียว"

วิธีทำให้รูปขึ้น: หน้า **อัลบั้มภาพ → ปุ่ม "จัดการรูป"**

- **ดึงรูปจากโฟลเดอร์ Drive** — ดึงทั้งโฟลเดอร์ทีเดียว (ต้องตั้ง `GOOGLE_API_KEY` ก่อน ดูด้านล่าง)
- **วางลิงก์รูปเอง** — วางลิงก์ Drive บรรทัดละ 1 อัน ใช้ได้เสมอ ไม่ต้องตั้งค่าอะไร
- สลับลำดับรูป · ตั้งรูปไหนเป็นปกก็ได้ · เอารูปออกจากอัลบั้ม (ไฟล์บน Drive ไม่ถูกลบ)

หน้าภาพรวมจะเตือนให้เองถ้ามีอัลบั้มไหนยังไม่มีรูปในระบบ

#### ตั้งค่า GOOGLE_API_KEY (ไม่บังคับ)

มีไว้ให้ปุ่ม "ดึงรูปจากโฟลเดอร์ Drive" ทำงาน ไม่ตั้งก็ใช้เว็บได้ปกติทุกอย่าง

1. [Google Cloud Console](https://console.cloud.google.com/) → สร้าง/เลือกโปรเจกต์
2. **APIs & Services → Library → Google Drive API → Enable**
3. **Credentials → Create credentials → API key**
4. กด **Restrict key** → API restrictions → เลือกเฉพาะ **Google Drive API**
   (ไม่จำกัดไว้ = คีย์เดียวใช้ยิง API ตัวอื่นในโปรเจกต์ได้หมด)
5. เอาค่าไปใส่ที่ Vercel → Settings → Environment Variables ชื่อ `GOOGLE_API_KEY` → redeploy

คีย์นี้อ่านได้เฉพาะโฟลเดอร์ที่ตั้งแชร์ "ทุกคนที่มีลิงก์" อยู่แล้ว เปิดไฟล์ส่วนตัวของใครไม่ได้
และ endpoint `/data?sheet=folder` เป็นการอ่านอย่างเดียว — คนที่ **เขียน**
รายชื่อรูปลงฐานข้อมูลยังต้องเป็นผู้ดูแลที่ล็อกอินแล้วเท่านั้น (RLS)


### รูปและไฟล์ยังอยู่บน Google Drive

ตอนนี้คอลัมน์ `cover_id` / `file_id` / `folder_id` ยังเก็บค่าที่ชี้ไป Google Drive
และไฟล์เหล่านั้น **เป็นของบัญชี Google ส่วนตัวของนักศึกษา**

แปลว่าถ้าบัญชีนั้นถูกปิด (เช่นเจ้าของเรียนจบ) **รูปทั้งหมดบนเว็บจะหายทันที**

จะแก้จบเมื่อย้ายไฟล์มา Supabase Storage ในเฟสถัดไป ระหว่างนี้อย่าเพิ่งปิดบัญชีนั้น

### ยังไม่ได้ใส่คนอื่นใน Supabase organization

ตอนนี้ organization มีคนเดียว ถ้าคนนั้นเข้าบัญชีไม่ได้ ก็ไม่มีใครจัดการฐานข้อมูลได้

**Organization Settings → Team → Invite** ใส่หัวหน้าฝ่ายไอทีหรือประธานชมรมไว้ด้วย

---

## เช็กลิสต์ตอนส่งต่อรุ่น

- [ ] เพิ่มอีเมลรุ่นใหม่ในตาราง `admins`
- [ ] ตั้งหัวหน้าฝ่ายไอทีรุ่นใหม่เป็น `owner` **ก่อน** ถอดคนเก่าออก
- [ ] ลบอีเมลรุ่นเก่าที่ไม่ได้ดูแลแล้วออก
- [ ] เชิญรุ่นใหม่เข้า Supabase organization
- [ ] เชิญรุ่นใหม่เข้า GitHub repo
- [ ] ส่งต่อรหัสบัญชีกลางของชมรม (Google) ให้ถูกคน
- [ ] เล่าให้ฟังว่า GitHub Action `backup-data` ทำอะไร — **ห้ามลบ** เพราะเป็นตัวสำรองข้อมูล

---

## ข้อมูลสำรอง

`data/backup/` เก็บสำเนาข้อมูลทั้งหมด อัปเดตอัตโนมัติทุกคืนตี 2
ดูรายละเอียดใน [data/backup/README.md](data/backup/README.md)
